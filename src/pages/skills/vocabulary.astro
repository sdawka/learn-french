---
import { query, queryOne } from "~/lib/db/index.ts";
import { getDueCount } from "~/lib/srs/scheduler.ts";
import { getProfile, proficiencyToCEFR } from "~/lib/learner-model/profile.ts";

const due = await getDueCount("vocabulary");
const profile = await getProfile();
const cefr = proficiencyToCEFR(profile.vocabulary_proficiency);

const totalKCs = (await queryOne<{ n: number }>(
  "SELECT COUNT(*) AS n FROM knowledge_components WHERE type = 'vocabulary'"
))?.n ?? 0;

const gameCounts = await query<{ game_type: string; total: number; correct: number }>(`
  SELECT r.game_type,
         COUNT(*) AS total,
         SUM(r.is_correct) AS correct
  FROM responses r
  JOIN knowledge_components kc ON kc.id = r.kc_id
  WHERE kc.type = 'vocabulary'
  GROUP BY r.game_type
`);
---

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Vocabulary — French</title>
</head>
<body class="bg-gray-950 text-white min-h-screen">
  <nav class="border-b border-gray-800 px-6 py-4 flex items-center gap-6">
    <a href="/" class="text-gray-400 hover:text-white text-sm">← Dashboard</a>
    <span class="font-semibold">Vocabulary</span>
    <span class="text-xs text-gray-500 ml-auto">{cefr}</span>
  </nav>

  <main class="max-w-3xl mx-auto px-4 py-8 space-y-8">
    <!-- Level + progress -->
    <section class="bg-gray-900 rounded-2xl p-6 border border-gray-800">
      <div class="flex items-center justify-between mb-4">
        <div>
          <h1 class="text-2xl font-bold">{cefr} Vocabulary</h1>
          <p class="text-sm text-gray-400 mt-1">{totalKCs} words in your deck</p>
        </div>
        <div class="text-right">
          <p class="text-3xl font-bold text-blue-400">{due.due}</p>
          <p class="text-xs text-gray-400">due today</p>
        </div>
      </div>
      <div class="h-2 bg-gray-800 rounded-full">
        <div
          class="h-full bg-blue-600 rounded-full"
          style={`width:${Math.min(100, (profile.vocabulary_proficiency / 6) * 100)}%`}
        ></div>
      </div>
    </section>

    <!-- Start session modes -->
    <section class="grid grid-cols-1 sm:grid-cols-3 gap-4">
      {[
        { mood: "review", label: "Review", desc: "Reinforce what you know", color: "blue" },
        { mood: "challenge", label: "Challenge", desc: "Push your limits", color: "red" },
        { mood: "explore", label: "Explore", desc: "Learn new words", color: "purple" },
      ].map(({ mood, label, desc, color }) => (
        <a
          href={`/games/vocabulary?mood=${mood}`}
          class={`bg-gray-900 border border-gray-800 hover:border-${color}-500 rounded-2xl p-5 transition-colors group`}
        >
          <p class={`font-semibold text-${color}-400`}>{label}</p>
          <p class="text-xs text-gray-400 mt-1">{desc}</p>
        </a>
      ))}
    </section>

    <!-- Game type accuracy breakdown -->
    {gameCounts.length > 0 && (
      <section class="bg-gray-900 rounded-2xl p-6 border border-gray-800">
        <h2 class="text-sm font-semibold text-gray-300 mb-4">Accuracy by game type</h2>
        <div class="space-y-3">
          {gameCounts.map(({ game_type, total, correct }) => {
            const pct = total > 0 ? Math.round((correct / total) * 100) : 0;
            return (
              <div>
                <div class="flex justify-between text-sm mb-1">
                  <span class="text-gray-300 capitalize">{game_type.replace("_", " ")}</span>
                  <span class="text-gray-400">{pct}% ({total})</span>
                </div>
                <div class="h-1.5 bg-gray-800 rounded-full">
                  <div
                    class="h-full bg-blue-500 rounded-full"
                    style={`width:${pct}%`}
                  ></div>
                </div>
              </div>
            );
          })}
        </div>
      </section>
    )}
  </main>
</body>
</html>
